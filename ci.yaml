actions:
    - ./configure --bootstrap-b2
    - live b2-config --target={{ target }} >> project-config.jam
    - ./b2 -d+2
    {% if target in ['macosx', 'linux'] %}
    - ./b2 tests//run
    {% endif %}
    - live c++-analysis ./b2 --build-dir=./bin/analysis -a
